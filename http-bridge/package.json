{
  "name": "@chaoschain/x402-bridge",
  "version": "0.1.0",
  "description": "HTTP bridge for ChaosChain x402 decentralized facilitator",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "start": "bun run src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1",
    "zod": "^3.23.8",
    "viem": "^2.21.0",
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "typescript": "^5.6.3"
  }
}

